const RunOption = {
    // 为1就是调试模式 为0就是正常模式
    debug: 1,
    removeAllEnemy: 0,
    // 强制排名模式地图ID，如果为-1的话就是不进行强制，ID从0开始，对应Config.mapList顺序
    fixedMapID: 0,
    // 是否显示死亡线
    showDeadLine: 0,
};
export default RunOption;
